{
    "buildCommand": "npm run dashboard:build",
    "outputDirectory": "packages/dashboard/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}